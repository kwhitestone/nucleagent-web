# nucleagent-web 主壳应用本地开发环境变量
# 复制为 .env.local 后按需修改。

# 主壳 dev 端口（默认 3000，生产 80）
WEB_PORT=3000

# 三个子应用来源（默认指向本地各子应用 dev server）
AUTH_WEB_URL=http://localhost:6678
CORE_WEB_URL=http://localhost:6688
EXECUTOR_WEB_URL=http://localhost:6698

# 主壳自身的后端代理目标（可选，主壳一般不直接调后端，由子应用各自代理）
